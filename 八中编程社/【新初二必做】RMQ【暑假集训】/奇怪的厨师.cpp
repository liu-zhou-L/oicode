#include <cstdio>

const int MAXN = 5e5 + 5;

int n, m, l, r, a[MAXN];

void rmq 

int main() {
	scanf("%d%d%d%d", &n, &m, &l, &r);
	for (int i = 1; i <= n; i++) {
		scanf("%d", &a[i]);
	}
	
	return 0;
}
