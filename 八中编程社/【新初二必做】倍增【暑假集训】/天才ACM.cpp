#include <cstdio>

int n, m, t, k, old_r;
long long a[MAXN];

int main() {
	scanf("%d", &k);
	while (k--) {
		scanf("%d%d%d", &n, &m, &t);
		for (int i = 1; i <= n; i++) {
			scanf("%lld", &a[i]);
		}
		int left, right;
		left = right = old_r = 1;
		b[left] = a[left];
		while (left <= n) {
			int k = 1;
			while (k)
		}
	}
	return 0;
}
